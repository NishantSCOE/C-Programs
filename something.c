#include <stdio.h>
int main()
{
    char names[], username[], surname[], access[];
    int mobNo[];
    const const int abcID = [] printf("Enter your name and surname: ");
    scanf("%s %s", names, surname);

    if (name == nishant || name == Nishant || name == NISHANT)
    {
        printf("What you want to access: ")
        {
            scanf("%s", access) if (access == ABC Id || access == abs id)
            {
                printf("ABC ID is %d", abcID);
            }
        }
    }
    return 0;
}